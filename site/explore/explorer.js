const searchInput=document.querySelector('#evidence-search');const filterButtons=[...document.querySelectorAll('.filter')];const cards=[...document.querySelectorAll('.evidence-card')];const resultCount=document.querySelector('#result-count');const emptyState=document.querySelector('#empty-state');let activeFilter='all';function applyFilters(){const query=(searchInput?.value||'').trim().toLowerCase();let visible=0;for(const card of cards){const matchesType=activeFilter==='all'||card.dataset.type===activeFilter;const haystack=(card.dataset.search||'').toLowerCase();const matchesQuery=!query||haystack.includes(query);const show=matchesType&&matchesQuery;card.hidden=!show;if(show)visible+=1}resultCount.textContent=`${visible} result${visible===1?'':'s'}`;emptyState.hidden=visible!==0}searchInput?.addEventListener('input',applyFilters);for(const button of filterButtons){button.addEventListener('click',()=>{activeFilter=button.dataset.filter||'all';for(const candidate of filterButtons){candidate.classList.toggle('active',candidate===button);candidate.setAttribute('aria-pressed',candidate===button?'true':'false')}applyFilters()})}applyFilters();