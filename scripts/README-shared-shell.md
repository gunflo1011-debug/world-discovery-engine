# Shared site shell

`apply-shared-site-shell.mjs` is the final presentation pass for generated HTML. Run it after the existing page generators so every page receives the same World Discovery header/navigation/footer shell while retaining page-specific content and metadata.
