# Worker 1 — Mexico Population 2025 implementation diff

_Date: 2026-09-17_

## CEO assignment
Return an exact, reviewable implementation diff for the stable `mexico population 2025` opportunity. Do not deploy or modify PR #208 / `/compare/null`.

## Stable evidence
CEO board baseline: Sep 14 `mexico population 2025` -> `/evidence/mexico-population-revision-2025/`: 5 impressions, average position 16.4, 0 clicks. The evidence page is archival/revision evidence for reference-year 2023, while the current population dataset exposes Mexico 2025 = 131,946,900. Preferred intent owner: `/countries/mex/`.

## Exact source path
English country profiles are generated by `scripts/build-wdi-country-hubs.mjs`. Localized profiles are generated separately by `scripts/build-localized-country-hubs.mjs`; they are intentionally out of scope.

## Proposed production diff (NOT APPLIED)
Inside the per-country loop in `scripts/build-wdi-country-hubs.mjs`, after `cards` is built, derive a Mexico-only answer from the existing population metric:

```diff
+  const populationAnswer = hub.code === 'MEX'
+    ? hub.metrics.find((m) => m.slug === 'population')
+    : null;
+  const intentAnswer = populationAnswer
+    ? `<section class="section"><div class="wrap"><p><strong>${esc(hub.country)}'s population in ${esc(populationAnswer.year)} is ${esc(formatValue(populationAnswer.value,populationAnswer.unit))}.</strong> <a href="../../data/population/">See the population ranking and compare countries →</a></p></div></section>`
+    : '';
```

Then insert only `${intentAnswer}` between the existing hero section and Quick View section:

```diff
- ...</div></section>${cards?`<section class="section section-soft">...
+ ...</div></section>${intentAnswer}${cards?`<section class="section section-soft">...
```

No value/year is hard-coded. The rendered answer must therefore be exactly `Mexico's population in 2025 is 131,946,900.` for the current official snapshot, and it automatically follows the existing verified population record if that snapshot changes in a future build.

## Expected generated HTML diff
Only `site/countries/mex/index.html` may change. Expected insertion immediately after the hero and before Quick View:

```html
<section class="section"><div class="wrap"><p><strong>Mexico's population in 2025 is 131,946,900.</strong> <a href="../../data/population/">See the population ranking and compare countries →</a></p></div></section>
```

No generated title, H1, meta description, canonical, structured data, cards, tables, sitemap entries, localized pages, evidence pages, or data-ranking pages should change because of this intervention.

## Blast radius / invariants
- 1 intended generated URL: `/countries/mex/`.
- 0 new URLs.
- English only; `/de/`, `/es/`, `/fr/`, `/zh-Hans/` unchanged.
- `/evidence/mexico-population-revision-2025/` unchanged.
- `/data/population/` unchanged.
- PRK/NCL Spanish ISO3 pilot unchanged.
- PR #208 and `/compare/null` untouched.
- Canonical remains `https://worlddiscoverydata.com/countries/mex/`.
- Hreflang is added later in the build pipeline by `enrich-english-hreflang.mjs`; this intervention does not alter its inputs or URLs, so hreflang output must remain identical in regression comparison.

## Required regression evidence before any merge/deploy
1. Clean build before/after and compare generated files; intervention-specific diff must be confined to `site/countries/mex/index.html` (ignoring unrelated generated timestamps only if already known/non-deterministic).
2. Assert the Mexico output contains exactly one `Mexico's population in 2025 is 131,946,900.` answer and one link to `../../data/population/`.
3. Assert a non-treatment English country does not contain the answer block.
4. Assert all localized Mexico country outputs do not contain the English answer block.
5. Assert Mexico title, H1, meta description and canonical are byte-identical before/after.
6. Assert `/evidence/mexico-population-revision-2025/` and `/data/population/` are byte-identical before/after.
7. Run `npm run build`, `npm run check:links`, and `npm test`; only proceed on green evidence.

## Recommendation to CEO
Approve this as the smallest useful pilot if/when deployment is authorized. It gives the preferred intent-owner page a direct answer using existing official data, while keeping the archival evidence page truthful and avoiding title/H1 churn or a sitewide country-template intervention.
