# Worker 1 country-taxonomy release evidence — 2026-09-13

Scope: PR #208 (`worker1/country-taxonomy-review-20260912`). Review/evidence only. **No production wiring, merge, or deployment.**

## Exact impact evidence

Source: successful PR CI run 1481, job `test` (103702646466), emitted by `test/wdi-country-groups.test.js` as `WDI_COUNTRY_TAXONOMY_IMPACT`.

- eligibleHubs: **217**
- affectedHubs: **217**
- movedLinks: **2,274**
- moved target groups:
  - People: **434**
  - Health: **626**
  - Energy & environment: **422**
  - Economy & work: **792**

Per-indicator moved-link counts:

| Indicator slug | Moved links | Target group |
|---|---:|---|
| population-age-0-14 | 217 | People |
| population-age-65-plus | 217 | People |
| birth-rate | 217 | Health |
| death-rate | 217 | Health |
| forest-area-share | 213 | Energy & environment |
| agricultural-land-share | 209 | Energy & environment |
| health-expenditure-share-of-gdp | 192 | Health |
| gdp | 186 | Economy & work |
| trade-share-of-gdp | 138 | Economy & work |
| exports-share-of-gdp | 138 | Economy & work |
| imports-share-of-gdp | 138 | Economy & work |
| fdi-net-inflows-share-of-gdp | 192 | Economy & work |

The per-indicator counts sum to 2,274; target-group counts also sum to 2,274.

## Representative hubs

The CI audit reports these examples from the real CURRENT_VERIFIED snapshots:

- **Afghanistan (AFG):** population-age-0-14 + population-age-65-plus → People; birth-rate + death-rate + health-expenditure-share-of-gdp → Health; forest-area-share + agricultural-land-share → Energy & environment.
- **Albania (ALB):** the two age indicators → People; birth-rate + death-rate + health-expenditure-share-of-gdp → Health; gdp + trade-share-of-gdp + exports-share-of-gdp + imports-share-of-gdp + fdi-net-inflows-share-of-gdp → Economy & work; forest-area-share + agricultural-land-share → Energy & environment.
- **Algeria (DZA):** same 12 movement types as Albania in this snapshot.

All movements are from `More indicators` to a visible topic.

## Scope proof

CI tests 205–208 pass on run 1481. The review test enforces that:

1. every CURRENT_VERIFIED indicator belongs to exactly one visible proposed topic;
2. the changed-slug set is exactly the intended 12 slugs listed above;
3. every intended moved slug starts in legacy `More indicators` and ends in a visible topic;
4. the impact audit reports exactly those 12 slugs and reconciles per-indicator/per-group totals with `movedLinks`;
5. duplicate slugs in either legacy or proposed grouping fail the test.

`life-expectancy` remains under **People** in both the effective legacy baseline and `WDI_COUNTRY_GROUPS`; it is not part of the moved set.

## SEO/data invariants for PR #208 as currently proposed

PR #208 deliberately does **not** wire `WDI_COUNTRY_GROUPS` into `build-wdi-country-hubs.mjs` or any production build stage. The PR changes only the new grouping source plus its review-only test/evidence. Therefore the successful full build on run 1481 is generated by the same production country-hub generator as the base implementation.

For the current draft, the proposed grouping source cannot change generated output, so these production outputs remain unchanged by PR #208 itself:

- **Country URLs:** unchanged; no route-generation code is wired to the new source.
- **Titles:** unchanged; no title/template generator is wired to the new source.
- **Canonicals:** unchanged; no canonical/SEO generator is wired to the new source.
- **Sitemap membership:** unchanged; no sitemap generator is wired to the new source. Run 1481 link audit passed with **1,553 sitemap pages**.
- **Indicator data values:** unchanged; the evidence audit reads existing `site/data/wdi/<slug>/data.json` snapshots and never writes them. No ingest/data generator is wired to the new source.

Run 1481 completed both full builds and both internal-link audits successfully; the final audit reported **115,325 internal HTML links and 0 orphan pages**. All **216 tests passed**.

Important release gate: these invariants prove the safety of the **current review-only PR**. If the taxonomy is later wired into the production country-hub generator, URL/title/canonical/sitemap/data invariants must be re-proved against actual before/after generated artifacts before deployment.

## Release status

**HOLD / DRAFT.** Evidence package is complete for CEO review, but this artifact does not authorize production wiring, merge, or deployment.
